spring:
  application:
    name: server-a
  zipkin:
    sender:
      type: rabbit
  sleuth:
    sampler:
      probability: 1.0

server:
  port: 8001
logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: debug


feign:
  package:
  - com.example.cloud.server.b.api.impl
  - com.example.cloud.server.b.api.impl2